---
version: 4
hierarchy:
    - name:    'Nodes'
      path:    "nodes/%{trusted.certname}"
      backend: 'yaml'

    - name:    'Roles'
      path:    "roles/%{trusted.extensions.pp_role}"
      backend: 'yaml'

    - name:    'Common-Local'
      path:    'local'
      backend: 'yaml'

    - name:    'Common'
      path:    'common'
      backend: 'yaml'

# vi:ts=4:sw=4:expandtab
